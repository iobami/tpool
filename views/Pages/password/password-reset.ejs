<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../../Templates/head') %>
    <link rel="stylesheet" href="../styles/custom-css/forgot-password.css">
    <style>
        #error, #error-currentPassword{
            width: 100%;
            align-items: center;
            /*border: 1px solid rgba(255, 0, 0, 0.329);*/
            margin: auto;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1em;
            display: none; 
  
        }
        #success-reset{
            width: 100%;
            align-items: center;
            /*border: 1px solid rgba(0, 255, 85, 0.377);*/
            margin: auto;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1em;
            display: none; 
        }
        #success-reset h5{
            color:rgba(0, 255, 85, 0.377);
            padding: 3px;
        }
        #error small,
        #error-currentPassword small{
          color: red;
          padding: 3px;
          
        }
  </style>
<script src="https://unpkg.com/axios/dist/axios.min.js"></script>

</head>

<body class="pass_reset-page">
    <!-- NAV-BAR -->
    <%- include('../../Templates/header') %>

    <div class="container-fluid">
        <div class="form-center">
            <h1>Reset Password</h1>
            <p>For the safety of your account, please enter a strong password</p>
            <form id="reset-password">
                <div class="" id="error-currentPassword" >
                    <small>There was an error in your request</small>
                </div>
                <div class="" id="success-reset" >
                    <h5>Password was reset successfully</h5>
                </div>
                <div class="form-group">
                    <label for="password">Enter New Password</label>
                    <h6 class="error"></h6>
                    <input type="password" class="form-control" id="password" placeholder="type password" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" title=" Password must have at least a capital letter, a small letter, a number and a symbol and must be at least 8 characters long." required>
                </div>
                <div class="form-group">
                    <label for="password">Retype New Password</label>
                    <h6 class="error"></h6>
                    <input data-val-type="confirmPassword" id="confirmPassword" type="password" class="form-control"
                        placeholder="retype password" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" title=" Password must have at least a capital letter, a small letter, a number and a symbol and must be at least 8 characters long." required>
                </div>
                <div class="" id="error" >
                    <small>Passwords do not match</small>
                  </div>
                <button type="submit" class="btn password-reset-btn btn-lg btn-block" id="resendPassBtn">Reset
                    Password</button>
            </form>
        </div>
    </div>
    <script src="../js/bootstrap-js/popper.js"></script>
    <script src="../js/bootstrap-js/jquery.js"></script>
    <script src="../js/bootstrap-js/bootstrap.js"></script>
    <script src="../js/custom-js/reset-password.js"></script>
</body>
</html>